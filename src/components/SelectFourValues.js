import React from 'react'
// 4本値のセレクトボックスを書く

const SelectFourValues = () => {
  return (
    <div>
      {/* 下のpタグをクリックしたらセレクトボックスが出てくる */}
      {/* その後pタグは隠れる */}
      <p>詳細な値を使う</p>
      <p>閉じる</p>
    </div>
  )
}

export default SelectFourValues